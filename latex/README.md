# Generated Paper Tables

Experiment-specific tables are generated into each completed run's `latex/` directory. Use `experiments/compare_runs.py` and `experiments/generate_report.py` for cross-run tables and reports.
